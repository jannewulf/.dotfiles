#!/usr/bin/env bash

ln -s "$DOTFILES"/bash/bashrc.symlink "$HOME"/.bashrc
ln -s "$DOTFILES"/bash/bash_profile.symlink "$HOME"/.bash_profile
ln -s "$DOTFILES"/bash/bash_aliases.symlink "$HOME"/.bash_aliases
